version: '3.8'
services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    # environment:
    restart: unless-stopped
    container_name: bigdata5-dashboard